// ─────────────────────────────────────────────────────────────────────────────
// إعدادات الأدمن (Admin Config)
// ─────────────────────────────────────────────────────────────────────────────
// فصلنا إعدادات الأدمن في ملف صغير مستقل عن كتالوج الأدوية الضخم،
// عشان أي ملف يستدعي ADMIN_EMAIL أو ROOT_ADMIN_UID ما يحتاجش يحمل كتالوج
// الأدوية كامل (اللي حجمه كبير جداً) في الـ bundle.

/**
 * البريد الإلكتروني الثابت للأدمن — تم تركه فارغاً بشكل متعمد.
 * السبب: قائمة الأدمن الحقيقية بتتحمل لحظياً من Firestore (collection: admins)،
 * عشان نقدر نضيف/نشيل أدمن من غير ما نعمل deploy جديد للتطبيق.
 * الحقل ده متبقي كـ legacy-fallback في بعض الشاشات بس، ومش بيأثر لو فاضي.
 */
export const ADMIN_EMAIL = '';

/**
 * UID الأدمن الجذري — يستخدم كطبقة أمان احتياطية في الواجهة (Client).
 * الحماية الحقيقية في firestore.rules و storage.rules (لازم يفضل نفس القيمة هناك).
 * مركزنا القيمة هنا عشان لو اتغيرت نعدل مكان واحد فقط.
 */
export const ROOT_ADMIN_UID = 'OrdU20b9pBXfUYrh4z8hNR0F14B2';
