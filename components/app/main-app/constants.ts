/**
 * ثوابت الواجهة الرئيسية للتطبيق (Main App Constants)
 *
 * يحتوي هذا الملف على الثوابت المرتبطة بسلوك الواجهة الرئيسية لـ `MainApp`:
 *   - مفتاح localStorage لتأجيل عرض طلب تفعيل الإشعارات.
 *   - المدة الزمنية المستخدمة عند اختيار "لاحقاً" في طلب الإشعارات.
 */

/** مفتاح localStorage المستخدم لحفظ وقت إخفاء طلب تفعيل الإشعارات */
export const PUSH_PROMPT_HIDE_UNTIL_KEY = 'dh_push_prompt_hide_until_main';

/** مدة إخفاء طلب الإشعارات عند النقر على "لاحقاً" — 12 ساعة */
export const PUSH_PROMPT_HIDE_MS = 12 * 60 * 60 * 1000;
